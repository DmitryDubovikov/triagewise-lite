# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env

# MLflow / Docker local state (mounted volumes)
mlflow-data/
# Stray local sqlite if a client ever runs against the default URI instead of Settings.
/mlflow.db
/mlruns/
