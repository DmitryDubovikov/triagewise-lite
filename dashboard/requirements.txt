# The dashboard image's lock — tiny and exact (analog of uv.lock). Versions shared with the
# host env are pinned to the same numbers as uv.lock, so the panel reads the stores with the
# same client dialect the app writes them with.
streamlit==1.59.1
mlflow-skinny==3.14.0
arize-phoenix-client==2.12.0
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
