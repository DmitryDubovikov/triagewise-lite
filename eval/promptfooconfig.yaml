# GENERATED by scripts/build_eval.py — do not edit by hand.
# source: data/golden.jsonl md5=7b60e7cdacfa47cb6841ff1c1d16407e (matches data/golden.jsonl.dvc)
description: Driftwood triage — CI eval gate (format/fields only)
prompts:
- file://prompts/champion.json
providers:
- id: openai:chat:gpt-4.1-nano-2025-04-14
  config:
    temperature: 0
defaultTest:
  assert:
  - type: is-json
    value:
      properties:
        category:
          title: Category
          type: string
        priority:
          enum:
          - low
          - medium
          - high
          - urgent
          title: Priority
          type: string
        sentiment:
          enum:
          - negative
          - neutral
          - positive
          title: Sentiment
          type: string
        needs_human:
          title: Needs Human
          type: boolean
        draft_reply:
          title: Draft Reply
          type: string
      required:
      - category
      - priority
      - sentiment
      - needs_human
      - draft_reply
      title: TriageResult
      type: object
tests: file://tests.yaml
